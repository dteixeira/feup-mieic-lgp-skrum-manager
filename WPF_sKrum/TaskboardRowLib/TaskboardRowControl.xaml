<UserControl x:Class="TaskboardRowLib.TaskboardRowControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             x:Name="UserControl"
             d:DesignHeight="600" d:DesignWidth="800" Foreground="Transparent">
    <Grid>
        <Grid x:Name="LayoutRoot" Background="Transparent">
            <ListView Width="Auto" HorizontalContentAlignment="Stretch" Height="Auto" ItemsSource="{Binding Tasks}" AllowDrop="True" BorderBrush="{x:Null}" Background="{x:Null}" BorderThickness="0" >
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="1" Width="auto" Height="auto" HorizontalAlignment="Stretch"  VerticalAlignment="Top" ScrollViewer.VerticalScrollBarVisibility="Hidden" ScrollViewer.HorizontalScrollBarVisibility="Hidden"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
            </ListView>
        </Grid>
    </Grid>
</UserControl>
